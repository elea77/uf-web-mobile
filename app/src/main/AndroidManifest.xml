<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uf_web_mobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ufwebmobile"
        android:usesCleartextTraffic="true">
        <activity android:name=".HistoryProductActivity"></activity>
        <activity android:name=".MyProductActivity" />
        <activity android:name=".ProductHistoryActivity" />
        <activity android:name=".AddProductActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".MyProductsListActivity" />
        <activity android:name=".ProductsListActivity" />
        <activity android:name=".MyAccountActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/ThemeMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>